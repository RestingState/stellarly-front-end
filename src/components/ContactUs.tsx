import { FC } from 'react';
// Components
import Header from './Header';

const ContactUs: FC = () => {
  return (
    <div>
      <Header active={true} />
      Contact us
    </div>
  );
};

export default ContactUs;
