export interface ICityRequestBody {
  name: string;
}

export interface ICityName {
  name: string;
}

export interface ICity {
  admin_name: string;
  id: number;
  latitude: string;
  longitude: string;
  name: string;
}
